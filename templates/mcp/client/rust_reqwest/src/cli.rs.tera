//! CLI module for {{ project_name }}
//!
//! This module provides the command-line interface for the MCP client.
//! The actual implementation is split into submodules for better organization.

// Re-export the cli module contents
pub use self::cli_impl::*;

// The actual CLI implementation is in the cli/ subdirectory
#[path = "cli/mod.rs"]
mod cli_impl;