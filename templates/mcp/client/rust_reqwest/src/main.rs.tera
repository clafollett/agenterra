//! {{ project_name }}
//!
//! {{ description }}
//! Version: {{ version }}

use anyhow::Result;
use {{ crate_name }}::cli;

#[tokio::main]
async fn main() -> Result<()> {
    // Delegate to CLI module for all command handling
    cli::execute().await?;
    Ok(())
}